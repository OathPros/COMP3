pages:
  stage: deploy
  needs: []
  script:
    - dnf install -y python3-pip
    - pip3 install --upgrade pip-tools
    - pip-compile
    - sphinx-build -b html source build
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master

